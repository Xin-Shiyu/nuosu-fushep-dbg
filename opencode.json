{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp", "--browser=msedge", "--allow-unrestricted-file-access"],
      "enabled": true,
      "env": { "ALLOW_UNRESTRICTED_FILE_ACCESS": "true" }
    }
  }
}
